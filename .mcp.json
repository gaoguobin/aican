{
  "mcpServers": {
    "aican": {
      "command": "python",
      "args": ["-m", "aican"],
      "env": {
        "PYTHONPATH": "${CLAUDE_PLUGIN_ROOT}/src",
        "ZLGCAN_SDK_PATH": "${CLAUDE_PLUGIN_ROOT}/zlgcan"
      }
    }
  }
}
